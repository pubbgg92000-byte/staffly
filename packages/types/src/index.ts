/**
 * @staffly/types — shared Zod schemas and TypeScript types.
 *
 * Boundary library: every request/response shape that crosses the wire is defined here
 * and consumed by both the API (validation pipe) and the clients (TanStack Query types).
 *
 * Subpath exports are added in feature sprints (see docs/03-api-specification.md).
 */
export const STAFFLY_TYPES_VERSION = "0.0.0";
