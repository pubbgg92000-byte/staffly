/**
 * @staffly/ui — shared design system.
 *
 * Batch 1 ships only the design tokens at `@staffly/ui/styles/tokens.css`.
 * Component primitives (Button, Input, AppShell, Sidebar, FormField, …) land in Batch 4.
 */
export const STAFFLY_UI_VERSION = "0.0.0";
